
#include"common.h"
#include<vector>
#include<algorithm>
using namespace std;

//Boyer-Moore  , a fantastic algorithm

vector<int> majorityElement(vector<int>& A) {
	vector<int> ret;
	if (A.size()) {
		int n = A.size();
	}
	return ret;
}