



using namespace std;
#include"common.h"


vector<pair<int, int>> kSmallestPairs(vector<int>& A, vector<int>& B, int k) {
	vector<int> f(A.size());
	int gain[]{A[0],B[0]};
	for (;;) {
		if (gain[0] > gain[1]) {
			
		}else{
			
		}
	}
}




